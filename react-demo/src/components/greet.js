export const Greet = (props) =>  {

return  (<div>
            <h1>Helow  {props.name} Hero Name {props.nickname}</h1>
           <h2>{props.children}</h2> 
           
         </div>
        ) 
}

//export default Greet
